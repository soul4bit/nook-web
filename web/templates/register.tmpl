<!doctype html>
<html lang="ru">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>{{.Title}} - {{.AppName}}</title>
  <link rel="icon" href="{{asset "favicon.svg"}}" type="image/svg+xml" />
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=IBM+Plex+Mono:wght@500;600&family=Manrope:wght@500;600;700;800&display=swap" rel="stylesheet" />
  <link rel="stylesheet" href="{{asset "app.css"}}" />
  <script src="{{asset "auth-live.js"}}" defer></script>
</head>
<body class="auth-page">
  <main class="auth-layout">
    <aside class="auth-aside">
      <p class="eyebrow">Knowledge Atlas</p>
      <h1 class="aside-title">Создайте узел знаний</h1>
      <p class="aside-text" data-auth-copy>
        Тут начинается культ полезных заметок: меньше «ща вспомню», больше «вот инструкция».
        Создайте аккаунт и собирайте свою DevOps-вики, пока инцидент еще не постучал в прод.
      </p>

      <div class="devops-scene" data-auth-scene>
        <div class="scene-shell">
          <span class="scene-line">$ terraform apply</span>
          <span class="scene-line">module.vpc: complete after 13s</span>
          <span class="scene-line">module.cluster: complete after 22s</span>
          <span class="scene-line">Outputs: endpoint = ready</span>
          <span class="scene-line scene-line-live" data-live-nick>input.nick: ожидание...</span>
          <span class="scene-line scene-line-live" data-live-email>input.email: ожидание...</span>
          <span class="scene-line scene-line-live" data-live-password>input.password: ожидание...</span>
          <span class="scene-line scene-line-live" data-live-confirm>input.confirm_password: ожидание...</span>
          <span class="scene-line scene-line-live" data-live-event>auth.event: ожидание...</span>
        </div>

        <div class="scene-flow" data-live-flow>
          <span class="flow-node flow-node-1"></span>
          <span class="flow-node flow-node-2"></span>
          <span class="flow-node flow-node-3"></span>
          <span class="flow-pulse" data-live-pulse></span>
        </div>

        <div class="scene-progress" role="presentation">
          <span class="scene-progress-label" data-live-status>статус: ожидание ввода</span>
        </div>
      </div>
    </aside>

    <section class="auth-card">
      <div class="brand-row">
        <div class="brand-mark brand-mark-image" aria-hidden="true"><img class="brand-mark-img" src="{{asset "logo-mark.svg"}}" alt="" /></div>
        <div>
          <p class="brand-title">{{.AppName}}</p>
          <p class="brand-subtitle">Регистрация</p>
        </div>
      </div>

      <nav class="auth-tabs" aria-label="Режим авторизации">
        <a href="/auth/login" class="auth-tab">Вход</a>
        <a href="/auth/register" class="auth-tab auth-tab-active">Регистрация</a>
      </nav>

      <h2 class="auth-title">Новый аккаунт</h2>
      <p class="auth-subtitle">Оставьте заявку: после одобрения придет письмо с подтверждением email.</p>

      {{if .Error}}
      <div class="alert alert-error">{{.Error}}</div>
      {{end}}

      <form method="post" action="/auth/register" class="auth-form" data-auth-form data-auth-mode="register">
        <input type="hidden" name="csrf_token" value="{{.CSRFToken}}" />
        <label class="field">
          <span>Ник</span>
          <input type="text" name="name" value="{{.Name}}" placeholder="@devops_nick" autocomplete="nickname" required />
        </label>

        <label class="field">
          <span>Email</span>
          <input type="email" name="email" value="{{.Email}}" placeholder="you@example.com" autocomplete="username" required />
        </label>

        <label class="field">
          <span>Пароль</span>
          <input type="password" name="password" placeholder="Минимум 10 символов" autocomplete="new-password" required />
          <small class="field-hint">Используйте уникальный пароль для этого сервиса.</small>
        </label>

        <label class="field">
          <span>Повторите пароль</span>
          <input type="password" name="confirm_password" placeholder="Повторите пароль" autocomplete="new-password" required />
        </label>

        <button class="button button-primary" type="submit">Отправить заявку</button>
      </form>

      <p class="footnote">
        Уже есть аккаунт?
        <a href="/auth/login">Войти</a>
      </p>
    </section>
  </main>
</body>
</html>
